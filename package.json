{
  "name": "notes",
  "version": "0.1.0",
  "author": "Jônatas Araújo <contato@jnaraujo.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "server-dev": "yarn workspace @notes/server dev",
    "server-build": "yarn workspace @notes/server build",
    "server-start": "yarn workspace @notes/server start",

    "web-dev": "yarn workspace @notes/web dev",
    "web-build": "yarn workspace @notes/web build",
    "web-start": "yarn workspace @notes/web start",

    "format": "yarn workspace @notes/web format && yarn workspace @notes/server format",
    "lint": "yarn workspace @notes/web lint && yarn workspace @notes/server lint"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
